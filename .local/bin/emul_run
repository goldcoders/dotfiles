#!/bin/sh
# Feed this script a link and it will give dmenu
# some choice programs to use to open it.

emulator -list-avds | dmenu -sb "#0088B2" -i -p "Please Choose A Device:" | xargs -r emulator -gpu auto -avd
